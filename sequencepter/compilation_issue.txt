Compilation issues

if linker complains
	- problems of include
		#if defined(ARDUINO) && ARDUINO >= 100
		#include "Arduino.h"
		#else
		#include "WProgram.h"
		#endif
		#include "Bounce_array.h"
	- problems of library with library <Wire.h>
		// #include <Wire.h>
		#include <i2c_t3.h>
		typedef i2c_t3 TwoWire;