#include "fct_clbk.h"

class fct_step: public fct_clbk {
	private:
		sequencer* _seq;
	
	public:
		fct_step();
		~fct_step();
		
		init(sequencer*);
		
		void on_push(uint8_t btn_id);
		void on_release(uint8_t btn_id);
		void update_ui();
}